# This file pins exact versions for a reproducible build on Python 3.13.
# The build will fail if these specific versions are not available or
# do not have pre-compiled wheels for Python 3.13 at build time.

# GUI Framework
PySide6==6.9.1

# EXE Bundler
pyinstaller==6.14.1

# Application Dependencies
pyttsx3==2.98
pywin32==310
sounddevice==0.5.2
scipy==1.16.0
numpy==2.3.1
SpeechRecognition==3.14.3
requests==2.32.4